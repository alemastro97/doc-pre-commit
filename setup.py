from setuptools import setup

setup(
    name='check-filetype',
    version='0.1.0',
    py_modules=['check_filetype'],
    install_requires=[
        # List your package's dependencies here
    ],
)
